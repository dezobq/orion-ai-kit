{
  "name": "orion-ai-kit",
  "version": "1.0.0",
  "description": "This kit gives you a minimal, **stack-agnostic** environment to run agentic coding workflows (Planner → Retrieve/Rerank → Implementer → Critic) with **Node+Python adapters** to start. It’s designed to be simple and extensible.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "tsc -p .",
    "test": "jest --reporters=default --reporters=jest-junit",
    "lint": "eslint .",
    "coverage": "jest --coverage --coverageReporters=cobertura --coverageReporters=lcov",
    "mutation": "stryker run"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@stryker-mutator/core": "^8",
    "@stryker-mutator/jest-runner": "^8",
    "@types/jest": "^30.0.0",
    "eslint": "^9.37.0",
    "istanbul-lib-coverage": "^3.2.2",
    "jest": "^29",
    "jest-junit": "^16",
    "ts-jest": "^29.4.4",
    "typescript": "^5"
  }
}
